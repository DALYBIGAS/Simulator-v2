# LLM Compiler Framework

This directory hosts the new compiler-oriented framework for LLM workloads.

- `kernels/`: kernel registry and kernel selection logic.
- `passes/`: target-aware pass pipeline planning.
- `runtime/`: ABI helpers and runtime launch planning.
- `profiler/`: gem5-style performance report builders.
